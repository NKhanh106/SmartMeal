// Re-export all card types from the canonical chatbot/types.ts file
// to keep card components import paths consistent
export type { CardType, CardOption, ChatCard, ChatCardResponse } from "@/components/chatbot/types";
