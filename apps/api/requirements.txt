# ───────────────────────────────────────────────────────────────────────────────
# SmartMeal API — Production Requirements
# Install with: pip install -r requirements.txt
# ───────────────────────────────────────────────────────────────────────────────

# Core framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# Database
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0
psycopg2-binary>=2.9.0

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==3.2.2
email-validator>=2.0.0

# Pydantic
pydantic>=2.0.0
pydantic-settings>=2.0.0

# AI
groq>=0.5.0
google-genai>=0.8.0
tenacity>=8.2.0

# Rate limiting
slowapi>=0.1.0

# Caching
redis[asyncio]>=5.0.0
hiredis>=2.3.0

# Scheduler
apscheduler>=3.10.0

# HTTP client
httpx>=0.27.0

# Utils
python-dotenv>=1.0.0

# Production
prometheus-fastapi-instrumentator>=6.1.0
# uvloop>=0.19.0  # Linux/macOS only — not available on Windows
# Install on Linux/macOS with: pip install uvloop
# Windows: pip install uvloop will fail, but the API runs fine without it

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
